export type Person = {
  id: string
  firstName: string
  lastName: string
  age: number
  visits: number
  status: string
  progress: number
  createdAt: Date
}

export type Product = {
  id: string
  name: string
  price: number
  stock: number
  category: string
  updatedAt: Date
}
